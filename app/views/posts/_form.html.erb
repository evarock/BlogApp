<%= form_with(model: [ @blog, @post ], local: true) do |form| %>
  <div class="field">
    <%= form.text_field :body, id: :post_body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
